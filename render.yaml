services:
  - type: web
    name: tzeva-dashboard
    env: python
    plan: free
    buildCommand: pip install -r requirements.txt
    startCommand: python tzeva_sirens_dashboard.py
    envVars:
      - key: PYTHON_VERSION
        value: 3.11.9